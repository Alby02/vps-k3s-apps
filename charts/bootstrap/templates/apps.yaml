{{- range $appName, $app := .Values.apps }}
---
apiVersion: v1
kind: Namespace
metadata:
  name: {{ $app.namespace }}
{{- if $app.database }}
---
# The basic-auth secret for PostgreSQL (CNPG) in pg-clusters namespace
apiVersion: v1
kind: Secret
metadata:
  name: {{ $appName }}-db-creds
  namespace: {{ $.Values.namespaces.pgCluster }}
  labels:
    cnpg.io/reload: "true"
type: kubernetes.io/basic-auth
stringData:
  username: {{ $app.database.user | quote }}
  password: {{ $app.database.password | quote }}
{{- end }}
{{- if $app.env }}
---
# The app env secret in the app's namespace
apiVersion: v1
kind: Secret
metadata:
  name: {{ default (print $appName "-env") $app.envSecretName }}
  namespace: {{ $app.namespace }}
type: Opaque
stringData:
{{- range $key, $val := $app.env }}
  {{ $key }}: {{ $val | quote }}
{{- end }}
{{- end }}
{{- end }}
